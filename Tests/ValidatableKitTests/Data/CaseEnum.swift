enum SingleCaseEnum: Int, CaseIterable {
    case one = 1
}

enum DoubleCaseEnum: Int, CaseIterable {
    case one = 1
    case two = 2
}

enum MultiCaseEnum: Int, CaseIterable {
    case one = 1
    case two = 2
    case three = 3
}
